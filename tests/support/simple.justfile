
# Build the project for the given target
build target:
    echo "Building for {{target}}"

# Deploy to the specified environment and version
deploy env version:
    echo "Deploying {{version}} to {{env}}"
